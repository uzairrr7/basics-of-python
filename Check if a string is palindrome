def is_palindrome(s):
    s = s.lower().replace(" ", "")  # optional: ignore case and spaces
    return s == s[::-1]

# Example usage:
s = input("Enter string: ")
if is_palindrome(s):
    print("Palindrome")
else:
    print("Not a palindrome")
