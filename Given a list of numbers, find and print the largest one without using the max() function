def find_largest(numbers):
    largest = numbers[0]
    for num in numbers:
        if num > largest:
            largest = num
    return largest

# Example
nums = [10, 25, 3, 99, 56]
print(find_largest(nums))
